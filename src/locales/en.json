{  
    "intro": "\nThis tool is made for converting credentials between the OpenBadges and the ELM format.\n\nIt takes a json file as input and outputs the new, mapped json file. Unmapped fields can be mapped manually. Manual mappings can be saved to a custom mapping file for future use.\n\nFor absolute paths start with '/', for relative paths the current directory is: %{pwd}",
    "input_path": "Input Path",
    "output_path": "Output Path",
    "choose_mapping": "Choose mapping",
    "choose_mapping_file": "Choose Mapping File",
    "save_custom_mapping": "Save Custom Mapping To",
    "language_selector": "Language Selector"
}
